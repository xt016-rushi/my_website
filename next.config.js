const { MIDDLEWARE_FILENAME } = require('next/dist/lib/constants');
const { Chakra_Petch, Volkhov } = require('next/font/google');
const { jsx } = require('react/jsx-runtime');

/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
};

module.exports = nextConfig;

  